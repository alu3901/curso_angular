import { ImgAsyncLoaderDirective } from './img-async-loader.directive';

describe('ImgAsyncLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgAsyncLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
